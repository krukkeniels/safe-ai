# Prevent CRLF corruption on Windows/WSL2 clones
* text=auto eol=lf

# Shell scripts — most critical (bad interpreter errors if CRLF)
*.sh text eol=lf

# Config files read by containers
*.yaml text eol=lf
*.yml text eol=lf
*.json text eol=lf
*.conf text eol=lf
*.toml text eol=lf

# Build files
Dockerfile text eol=lf
Makefile text eol=lf
